<!DOCTYPE html>
<html>
  <head>
    <title>RetreatGame</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= yield :js %>
  </head>
  <style>

    .board {
      display: grid;
      grid-template-columns: repeat(6, 1fr);
      grid-gap: 0px;
      grid-auto-rows: minmax(128px, auto);
    }

    canvas {
      background-image: url('<%= asset_url("/assets/tiles/dirt.png") %>');
      background-repeat: repeat;
    }

    .board .cell {
      height: 128px;
      width: 128px;
    }

    .board .center {
      width: 800px;
    }

    .board .middle {
      height: 600px;
    }

    .board .top.left {
      border-radius: 50% 0% 0% 0%;
      background-image: url('<%= asset_url("/assets/tiles/top_left_corner.png") %>');
      grid-column: 1 / 3;
      grid-row: 1;
    }

    .board .top.center {
      background-image: url('<%= asset_url("/assets/tiles/top.png") %>');
      background-repeat: repeat-x;
      grid-column: 2 / 3;
      grid-row: 1;
    }

    .board .top.right {
      border-radius: 0% 40% 0% 0%;
      background-image: url('<%= asset_url("/assets/tiles/top_right_corner.png") %>');
      grid-column: 3 / 3;
      grid-row: 1;
    }

    .board .middle.left {
      background-image: url('<%= asset_url("/assets/tiles/left_side.png") %>');
      background-repeat: repeat;
      grid-column: 1 / 3;
      grid-row: 2;
    }

    .board .middle.center {
      background-image: url('<%= asset_url("/assets/tiles/dirt.png") %>');
      background-repeat: repeat;
      grid-column: 2 / 3;
      grid-row: 2;
      height: 600px;
      width: 800px;
    }

    .board .middle.right {
      background-image: url('<%= asset_url("/assets/tiles/right_side.png") %>');
      background-repeat: repeat-y;
      grid-column: 3 / 3;
      grid-row: 2;
    }
    
    .board .bottom.left {
      border-radius: 0% 0% 0% 50%;
      background-image: url('<%= asset_url("/assets/tiles/bottom_left_corner.png") %>');
      background-repeat: repeat-y;
      grid-column: 1 / 3;
      grid-row: 3;
    }

    .board .bottom.center {
      background-image: url('<%= asset_url("/assets/tiles/bottom.png") %>');
      background-repeat: repeat;
      grid-column: 2 / 3;
      grid-row: 3;
    }

    .board .bottom.right {
      border-radius: 0% 0% 50% 0%;
      background-image: url('<%= asset_url("/assets/tiles/bottom_right_corner.png") %>');
      grid-column: 3 / 3;
      grid-row: 3;
      background-repeat: repeat-y;
    }

  </style>

  <body>
    <nav class="navbar navbar-expand navbar-light bg-light">
      <a class="navbar-brand" href="/">💚Retreat 2020</a>
      <div>
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="/">Board</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/teams">Teams</a>
          </li>
        </ul>
      </div>
    </nav>
    <br />
    <br />
    <div class="container-fluid">
      <%= yield %>
    </div>
  </body>
</html>
